# Copy to .env (gitignored) and fill in your Nexus credentials, as an alternative to
# exporting NEXUS_USERNAME/NEXUS_PASSWORD in the shell. Used by dataset auto-provisioning
# (see README.md#dataset-auto-provisioning) to authenticate against the restricted Nexus
# dataset repository.
NEXUS_USERNAME=
NEXUS_PASSWORD=
